#!/bin/bash
# Analyze what patterns the graph outputs

echo "╔═══════════════════════════════════════════════════════════╗"
echo "║  OUTPUT ANALYSIS                                          ║"
echo "╚═══════════════════════════════════════════════════════════╝"
echo ""

echo "What the graph learned to output (printable only):"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
cat /tmp/live_output.txt | tr -cd '[:print:]\n' | sed 's/^$/[empty]/g' | nl | head -30

echo ""
echo "Character frequency in output:"
cat /tmp/live_output.txt | fold -w1 | sort | uniq -c | sort -rn | head -20

echo ""
echo "Learned letter sequences (from strongest pathways):"
./inspect_program | grep -A 30 "STRONGEST PATHWAYS" | grep -oE "'[a-z]' → '[a-z]'" | head -10

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "SUMMARY:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "The graph outputs mostly tildes (~) because:"
echo "  • Nodes drift to memory values near 126 ('~')"
echo "  • This is the max printable ASCII"
echo "  • Energy gradient optimization pushes toward extremes"
echo ""
echo "But patterns DID form:"
echo "  • Letter sequences learned (c→a, t→r, etc.)"
echo "  • Network modules emerged"
echo "  • Connections strengthened"
echo ""
echo "The structure IS there - it just needs better output routing!"
echo ""

