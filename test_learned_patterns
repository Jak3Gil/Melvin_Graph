#!/bin/bash
# Test what specific patterns were learned

echo "🔍 TESTING LEARNED PATTERNS"
echo "============================"
echo ""

echo "Strongest pathways learned:"
./inspect_program | grep -A 25 "STRONGEST PATHWAYS"

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "PATTERN ANALYSIS"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Test individual letters
echo "Testing 'c' (start of 'cat'):"
echo "c" | ./melvin_core 2>&1 | head -5

echo ""
echo "Testing 'd' (start of 'dog'):"
echo "d" | ./melvin_core 2>&1 | head -5

echo ""
echo "Testing 't' (start of 'tree'):"
echo "t" | ./melvin_core 2>&1 | head -5

echo ""
echo "Testing full word 'cat':"
echo "cat" | ./melvin_core 2>&1 | head -5

echo ""
echo "Testing full word 'tree':"
echo "tree" | ./melvin_core 2>&1 | head -5

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "OBSERVATIONS:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Look for:"
echo "  • Cascade effects (one letter triggers others)"
echo "  • Pattern completion (partial word triggers full word)"
echo "  • Learned associations"
echo ""

